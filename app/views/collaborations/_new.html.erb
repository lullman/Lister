<%= form_for(:collaboration, url: user_collaborations_path(user_id: current_user, id: list.id)) do |f| %>
  <%= f.hidden_field :source, value: "collaboration" %>
  <%= f.submit "Contribute" %>
<% end %>